@page "/search-results"@rendermode InteractiveServer<FilterOptionsComponent></FilterOptionsComponent><RadzenRow Gap="5" RowGap="5">    <RadzenColumn Size="12">        @Query    </RadzenColumn></RadzenRow><script>    function BlazorSetTitle(title, filter) {        let titleValue = "";        if (title != null)            titleValue += " - " + title;        if (filter != null)            titleValue += " - " + title;        document.title = "MyVideoResu.ME - Results" + titleValue;    }</script>